package com.mobigesture.oms.item.service.services;

import org.springframework.boot.test.context.SpringBootTest;


public class ItemServiceTest {
	
	/*
	 * @Mock private ItemRepository itemRepository;
	 * 
	 * @InjectMocks private ItemService itemService = new ItemServiceImpl();
	 * 
	 * @Test void shouldReturnFindAll() { List<ProductDTO> datas = new ArrayList();
	 * datas.add(new ProductDTO("1011", "Mouse",1)); datas.add(new
	 * ProductDTO("1022", "KeyBoard",1)); datas.add(new ProductDTO("3033",
	 * "Monitor",1));
	 * 
	 * given(itemRepository.findAll()).willReturn(datas);
	 * 
	 * List<ProductDTO> expected = itemService.retriveOrderItem();
	 * 
	 * assertEquals(expected, datas); }
	 */
}
